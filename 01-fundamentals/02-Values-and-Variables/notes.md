#Values and Variables

value is basically a piece of data
eg: age, name, 5, true

variables are used to store these values



// standard to write variable names

//camelCase

eg:
let firstName = "Ankit";

// Naming variables

- cant start with a number: 3name is invalid
- can contain number, letter, _ , and doller sign $,  : age3 is valid
- can not we a keyword {keywords are reservedd words} like function: let function = "hey"; is invalid
- should not start variable name with capital: its just convention so its valid


const is used for contant values, values that never changes
const grades = 89;